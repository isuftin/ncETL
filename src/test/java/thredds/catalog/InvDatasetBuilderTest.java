package thredds.catalog;

import org.junit.After;
import org.junit.Before;
import org.junit.Test;
import static org.hamcrest.MatcherAssert.*;
import static org.hamcrest.Matchers.*;
/**
 *
 * @author Ivan Suftin <isuftin@usgs.gov>
 */
public class InvDatasetBuilderTest {
    
    public InvDatasetBuilderTest() {
    }

    @Before
    public  void setUp() throws Exception {
    }

    @After
    public  void tearDown() throws Exception {
    }

    @Test
    public void testSomeMethod() {
//        InvDatasetBuilder test = new InvDatasetBuilder("test");
//        test.id("testId")
//            .collectionType("testCollectionType")
//            .contributor("testName", "testRole");
//        assertThat(test.getInternalInvDataset().getID(), is(equalTo("testId")));
//        assertThat(test.getInternalInvDataset().getCollectionType(), is(notNullValue()));
//        assertThat(test.getInternalInvDataset().getContributors().get(0).getName(), is(equalTo("testName")));
//        assertThat(test.getInternalInvDataset().getContributors().get(0).getRole(), is(equalTo("testRole")));
    }
}
